# PROMPTS.md

This file lists representative AI prompts used while building this project.

1. *“Generate a minimal Cloudflare Workers + Durable Objects chat backend that calls Workers AI (Llama 3.x) and stores memory. Expose `/api/chat` and serve static frontend from `/`. Use TypeScript modules, wrangler.toml bindings, and a MemoryDO class.”*

2. *“Write a simple Pages-style frontend with a chat box, streaming feel (append-as-received), and a microphone button using the Web Speech API for voice-to-text.”*

3. *“Design a memory strategy: retain last N verbatim turns and keep a rolling summary in DO; include a maintenance method to refresh summary after each turn.”*

4. *“Draft a README with architecture, setup, deploy, and model configuration notes.”*
